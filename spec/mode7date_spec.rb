require 'spec_helper'

describe Mode7date do
  it 'has a version number' do
    expect(Mode7date::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
